@model IEnumerable<MichellesWebsite.Models.CultureCountry>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.country)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.culture)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.country)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.culture)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CultureCountryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CultureCountryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CultureCountryId })
        </td>
    </tr>
}

</table>
